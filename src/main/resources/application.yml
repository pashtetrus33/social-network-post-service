spring:
  application:
    name: post-service

  jpa:
    generate-ddl: false
    open-in-view: false
  datasource:
    url: jdbc:postgresql://localhost:5432/post_db?currentSchema=post_schema
    #url: jdbc:postgresql://91.197.98.213:5432/post_db?currentSchema=post_schema
    username: postgre_user
    password: postgre_secret_password
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 10000


  liquibase:
    change-log: classpath:/db/changelog/master.yml


eureka:
  client:
    serviceUrl:
     defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
     #defaultZone: http://host.docker.internal:8761/eureka/

  instance:
    preferIpAddress: true

server:
  port: 8082